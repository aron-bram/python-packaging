# this line makes sure the package user doesn't have to import board as 
# `from ranboardom.script import board` but rather `from ranboardom import board`

from ranboardom.script import board # ignore